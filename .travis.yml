# seems that travis supports D, using dub with various compilers
language: d

d:
  - dmd
  - dmd-2.068.2
  - ldc
  - ldc-0.15.1
  - gdc
  - gdc-4.9.2

os:
 - linux
 - osx

sudo: false

script: dub build

addons:
  apt:
    packages:
    - libev-dev
    - libevent-dev
    - libsqlite3-dev
